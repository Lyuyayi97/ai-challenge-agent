{
    "jigsaw": "【拼图任务核心规则】\n1. 你面前的区域分为三部分：左侧是参考图，中间是已放置正确的拼图块（有6个），右侧是待放置的拼图块（3个）。\n2. 每个空缺位置的X坐标固定为837，你只需要确定Y坐标。参考图中空缺位置的Y坐标就是目标Y坐标。\n3. 操作流程：\n   a. 先用 move_to_location 移动到观察点 X=750, Y=191，然后使用 look_at_location 或 turn_in_degree 调整视角，直到能清楚看到参考图和拼图区域。\n   b. 识别右侧待放置拼图块的形状，与参考图中的空缺位置进行匹配。\n   c. 确认匹配后，使用 move_and_take_object 拿起对应的拼图块（使用正确的object_id）。\n   d. 使用 put_down_to_location 放置到目标位置，其中 target_location.X = 837，target_location.Y = 参考图中对应的Y坐标，target_location.Z = 根据拼图块高度适当调整（建议比地面高2-5个单位）。\n   e. 如果放置后位置不对，尝试旋转角度：使用 put_down_to_location 的 target_rotation 参数，依次尝试 yaw = 0, 90, 180, 270。\n4. 如果拼图块不在视野内，移动视角或转身寻找，不要盲目猜测。\n5. 当你确认所有3个空缺都已正确填充后，使用 finish_task 完成任务。",
    "tidyroom": "【整理房间任务核心规则】\n1. 你需要整理的物品是：抱枕、鞋子、垃圾、食物、杯子。注意：人脚上穿的鞋子（通常成对出现在人旁边）不是散乱物品，不要拿取。散乱的鞋子通常单独出现在地面或角落。\n2. 操作流程：\n   a. 先环顾四周，识别场景中所有可见物体，找出散乱的物品。\n   b. 抓取物品时，使用 move_and_take_object，参数 object_id 必须来自 perception 返回的可见物体列表，不要自己编ID。\n   c. 放置物品时：\n      - 杯子 → 放到茶几或桌面上，放置高度 (Z) 为桌面顶部高度 + 2（略高于表面）\n      - 抱枕 → 放到沙发或床上的正确位置\n      - 食物/垃圾 → 放到垃圾桶或指定区域\n      - 鞋子 → 放到鞋柜上或鞋架处\n   d. 放置时使用 put_down_sth 或 put_down_to_location，target_location.Z 要略高于目标表面（避免穿模），通常比物体包围盒的Z最大值高2-5个单位。\n3. 错误恢复：\n   - 如果抓取失败（result: failed），换一个物体尝试，不要重复同一个动作超过2次。\n   - 如果某个物体已经整理好，不再重复操作它。\n4. 当你确认所有散乱物品都已整理到位后，使用 finish_task 完成任务。",
    "npc": "【NPC对话任务核心规则】\n1. 可对话的NPC只有四人：江淑艳(jiangshuyan)、刘伟东(liuweidong)、赵爷爷(zhaoyeye)、张奶奶(zhangnainai)。\n2. 操作流程：\n   a. 首先找到目标NPC，确保NPC在你的视野内。\n   b. 使用 speak_to_npc 接口，参数 target 必须是上述四人之一的全名（或括号内的英文名），content 是你想说的话。\n   c. 每次对话后，观察NPC的回复（从 perception 或 task_response 中获取），根据回复内容决定下一步说什么。\n   d. 对话是任务驱动的，你需要通过对话获取关键信息或完成某个目标（如获取密码、了解背景等）。\n3. 对话策略：\n   - 开场时，先打招呼并介绍自己。\n   - 根据NPC的回复，追问具体问题。\n   - 如果NPC给出关键信息，记录并确认。\n   - 不要重复问相同的问题超过3次。\n4. 当你通过对话获取了所有需要的信息，或NPC明确表示对话结束后，使用 finish_task 完成任务。注意：不是答完一道题就结束，要完成完整的对话流程。",
    "raven": "【分类计数任务核心规则】\n1. 你需要统计场景中特定类别物体的数量（如红色物体、圆形物体、特定颜色的物品等）。\n2. 操作流程：\n   a. 先在原地环顾一周，使用 turn_in_degree 旋转360度，让所有物体进入视野。\n   b. 从 perception 返回的 objects 列表中，提取目标类别的物体。注意：每个 object_id 对应一个唯一物体，不要重复计数。\n   c. 如果视野不够全面，移动到房间中心或其他位置重新观察，确保没有遗漏。\n3. 常见陷阱：\n   - 物体可能被遮挡，需要移动视角才能看到。\n   - 部分物体颜色/形状相似但属于不同类别，需要严格按题目要求判断。\n   - 只计数可见物体，不要猜测看不见的物体数量。\n4. 确认计数无误后，使用 submit_answer，参数 output 只写最终数字（纯数字，不要带其他文字）。\n5. 如果题目要求多种类别的计数（如红色圆形有几个），分别计数后一并提交。",
    "counting": "瑞文测试任务核心规则】\n1. 瑞文测试是图形推理题：你会看到一系列图形，找出规律并选择正确的答案。\n2. 操作流程：\n   a. 使用 solve_raven 接口来解答当前题目。该接口会自动读取当前题目中的瑞文图片并返回候选答案。\n   b. 不要自己尝试移动或操作图形，只需要调用 solve_raven 接口获取答案。\n   c. 如果 solve_raven 返回的结果不明确，可以尝试调整视角，确保图形清晰可见后再调用一次。\n3. 提示：\n   - 瑞文测试的规律通常涉及：形状变化、颜色变化、数量变化、旋转、对称等。\n   - 不需要进行复杂的物理操作，核心是推理和选择正确选项。\n4. 确认答案后，使用 submit_answer 提交，output 写你选择的答案编号（如 'A'、'B'、'C'、'D'）。"
}